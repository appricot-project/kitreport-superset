export const MAX_FILE_SIZE = 50 * 1024 * 1024; // 50 МБ
export const MAX_FILE_SIZE_MB = MAX_FILE_SIZE / (1024 * 1024);
export const MAX_FILES_COUNT = 20;
export const MAX_TOTAL_SIZE = 200 * 1024 * 1024; // 200 МБ
export const MAX_TOTAL_SIZE_MB = MAX_TOTAL_SIZE / (1024 * 1024);
